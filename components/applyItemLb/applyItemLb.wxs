var formatState = function (str) {
  if (str) {
    if (str == '1') {
      return '待审核'
    } else if (str == '2') {
      return '已拒绝'
    } else if (str == '3') {
      return '已通过'
    } 
  }
}
module.exports = {
  formatState: formatState
}