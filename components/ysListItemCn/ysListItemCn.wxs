var formatState = function (str) {
  if (str) {
    if (str == '1') {
      return '未提交'
    } else if (str == '2') {
      return '已提交'
    } else if (str == '3') {
      return '被拒收'
    } else if (str == '4') {
      return '被收款'
    }
  }
}
module.exports = {
  formatState: formatState
}